# Environment Configuration Example
# Copy this file to .env for local development

# Application Settings
NODE_ENV=development
PORT=3000
HOSTNAME=0.0.0.0

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# Redis Cache Configuration (Optional)
# Set REDIS_ENABLED=true to enable Redis caching
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379
REDIS_PREFIX=pudim:
REDIS_TTL=3600
REDIS_CIRCUIT_BREAKER_COOLDOWN=60000

# Redis Configuration Details:
# - REDIS_ENABLED: Enable/disable Redis caching (true/false)
# - REDIS_URL: Redis connection string
#   - Local: redis://localhost:6379
#   - Docker: redis://redis:6379
#   - Remote: redis://username:password@host:port
# - REDIS_PREFIX: Namespace prefix for all cache keys
# - REDIS_TTL: Time-to-live in seconds (default: 3600 = 1 hour)
# - REDIS_CIRCUIT_BREAKER_COOLDOWN: Cooldown period in milliseconds after Redis failures (default: 60000 = 1 minute)

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Start Redis: docker-compose up redis -d
# 3. Enable Redis in .env: REDIS_ENABLED=true
# 4. Run the app: npm run dev
