# Environment Configuration Example
# Copy this file to .env for local development

# Application Settings
NODE_ENV=development
PORT=3000
HOSTNAME=0.0.0.0

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# Redis Cache Configuration (Optional)
# Set REDIS_ENABLED=true to enable Redis caching
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379
REDIS_PREFIX=pudim:github:
REDIS_TTL=300
REDIS_CIRCUIT_BREAKER_COOLDOWN=300000

# Redis Configuration Details:
# - REDIS_ENABLED: Enable/disable Redis caching (true/false)
# - REDIS_URL: Redis connection string
#   - Local: redis://localhost:6379
#   - Docker: redis://redis:6379
#   - Remote: redis://username:password@host:port
# - REDIS_PREFIX: Namespace prefix for all cache keys
# - REDIS_TTL: Time-to-live in seconds (default: 300 = 5 minutes)
# - REDIS_CIRCUIT_BREAKER_COOLDOWN: Cooldown period in milliseconds after Redis failures (default: 300000 = 5 minutes)

# DynamoDB Score Storage Configuration (Optional)
# Set DYNAMODB_ENABLED=true to enable DynamoDB score storage
DYNAMODB_ENABLED=false
DYNAMODB_ENDPOINT=http://localhost:8000
DYNAMODB_CIRCUIT_BREAKER_COOLDOWN=300000
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local

# DynamoDB Configuration Details:
# - DYNAMODB_ENABLED: Enable/disable DynamoDB score storage (true/false)
# - DYNAMODB_ENDPOINT: DynamoDB endpoint URL
#   - Local: http://localhost:8000 (for DynamoDB Local)
#   - Docker: http://dynamodb:8000 (when running in Docker network)
#   - AWS: Omit this variable to use AWS DynamoDB
# - DYNAMODB_CIRCUIT_BREAKER_COOLDOWN: Cooldown period in milliseconds after DynamoDB failures (default: 300000 = 5 minutes)
# - AWS_REGION: AWS region (default: us-east-1)
# - AWS_ACCESS_KEY_ID: AWS access key (use "local" for DynamoDB Local)
# - AWS_SECRET_ACCESS_KEY: AWS secret key (use "local" for DynamoDB Local)

# Leaderboard Feature Configuration (Optional)
# Requires DYNAMODB_ENABLED=true to function
# Set LEADERBOARD_ENABLED=true to enable leaderboard feature
LEADERBOARD_ENABLED=false

# Leaderboard Configuration Details:
# - LEADERBOARD_ENABLED: Enable/disable leaderboard feature (true/false)
#   - Requires DYNAMODB_ENABLED=true to function
#   - When enabled, displays top 10 scores on homepage
#   - Users must give explicit consent to appear on leaderboard
#   - Only scores with leaderboard_consent=true are displayed

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Start services: docker-compose up redis dynamodb -d
# 3. Enable services in .env:
#    - REDIS_ENABLED=true (for caching)
#    - DYNAMODB_ENABLED=true (for score storage)
#    - LEADERBOARD_ENABLED=true (for leaderboard, requires DYNAMODB_ENABLED=true)
# 4. Run the app: npm run dev
#
# Example configurations:
#
# Basic setup (no caching or storage):
#   REDIS_ENABLED=false
#   DYNAMODB_ENABLED=false
#   LEADERBOARD_ENABLED=false
#
# With caching only:
#   REDIS_ENABLED=true
#   DYNAMODB_ENABLED=false
#   LEADERBOARD_ENABLED=false
#
# With storage only:
#   REDIS_ENABLED=false
#   DYNAMODB_ENABLED=true
#   LEADERBOARD_ENABLED=false
#
# Full setup (caching + storage + leaderboard):
#   REDIS_ENABLED=true
#   DYNAMODB_ENABLED=true
#   LEADERBOARD_ENABLED=true
